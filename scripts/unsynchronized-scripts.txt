# 存放多用户多端开发后需要同步的脚本

alter table auth_user
add column email_address varchar(64) not null default '' comment '邮箱地址';
alter table auth_user
add index idx_email_address (email_address);

  email:
    access-key-id: a
    access-key-secret: b
    account-name: c


# gateway Nacos

      - /auth-service/register/getPhoneVerificationCode
      - /auth-service/register/getEmailVerificationCode

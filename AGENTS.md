# AGENTS.md - iw-mixes 历史归档项目指南

## 项目定位

`iw-mixes` 是 IW 系统旧后端归档项目，不再作为默认开发入口。当前后端迭代统一进入 `../iw-mixes-server`，用户说 `后端` 时也默认指向 `../iw-mixes-server`。

本项目只用于：

- 追溯旧后端实现。
- 对照迁移前接口、字段、SQL 或业务逻辑。
- 用户明确要求维护历史归档内容时，才进行编辑。

## 路由规则

- 新接口、新字段、新服务配置、新发布链路：进入 `../iw-mixes-server`。
- 微信小程序相关需求：进入 `../iw-mixes-app-wx`。
- Web 管理端或用户单独说 `前端`：进入 `../iw-mixes-web-platform`。
- 只有用户明确说要查看或修改 `iw-mixes` 旧项目时，才在本目录操作。

## 只读参照原则

- 默认不要修改本项目代码、配置、脚本或构建文件。
- 不要在本项目新增业务能力来满足当前生产需求。
- 不要按旧网关、Nacos、RocketMQ、Spring Cloud Gateway 架构推断新后端；新后端以 `../iw-mixes-server/AGENTS.md` 为准。
- 读取旧实现时，要在最终说明中明确哪些结论来自归档项目，哪些改动落在新项目。

## 禁止触碰

- `target/`
- `.git/`
- IDE 缓存目录
- 本地敏感配置和真实密钥

如果确实需要修改归档内容，先确认用户明确要求的是历史归档维护，而不是新后端需求。
